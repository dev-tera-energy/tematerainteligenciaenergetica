{# ==========================================================
   Rótulo mono caixa-alta com filete ciano.
   Parâmetros: texto (string), on_dark (bool, opcional).
   ========================================================== #}
<span class="t-eyebrow {{ on_dark ? 'on-dark' : '' }}">{{ texto }}</span>
